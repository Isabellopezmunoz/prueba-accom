---
interface Props {
  class?: string
}

const { class: className = "" } = Astro.props
---

<span
  class:list={[
    "inline-block rounded-full bg-accent-soft px-3 py-1 text-center text-sm font-semibold uppercase tracking-wider text-accent-dark",
    className,
  ]}
>
  <slot />
</span>
